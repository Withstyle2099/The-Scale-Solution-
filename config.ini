# LSI Dashboard Configuration File

[PATHS]
# Data file location
data_file = C:\Users\UM_AS\Downloads\AI_LSI_Demo_Historical_Data.csv

[STREAMLIT_CONFIG]
# Port number for the web interface
port = 8501

# Theme (light or dark)
theme = light

# Page layout (wide or centered)
layout = wide

[PREDICTION_PARAMETERS]
# Default values for prediction inputs

# Temperature (Celsius)
temp_min = 30.0
temp_max = 45.0
temp_default = 38.6

# Flow Rate (m³/h)
flow_min = 1100.0
flow_max = 1250.0
flow_default = 1182.8

# pH
ph_min = 6.5
ph_max = 7.5
ph_default = 7.09

# Calcium (mg/L)
calcium_min = 80.0
calcium_max = 115.0
calcium_default = 97.3

# Alkalinity (mg/L)
alkalinity_min = 170.0
alkalinity_max = 200.0
alkalinity_default = 185.5

# TDS (mg/L)
tds_min = 27500.0
tds_max = 30000.0
tds_default = 28726.2

[MODEL_PARAMETERS]
# Machine learning model hyperparameters

# Random Forest
rf_estimators = 100
rf_max_depth = 15
rf_random_state = 42

# Gradient Boosting
gb_estimators = 100
gb_max_depth = 5
gb_learning_rate = 0.1
gb_random_state = 42

# Test/Train Split
test_size = 0.2
random_state = 42

[LSI_THRESHOLDS]
# LSI interpretation thresholds

# Scaling risk threshold (above this value = scaling risk)
scaling_threshold = 0.1

# Corrosion risk threshold (below this value = corrosion risk)
corrosion_threshold = -0.15

# Balanced range = between corrosion and scaling thresholds

[DISPLAY_SETTINGS]
# Chart and visualization settings

# Plot size
plot_width = 10
plot_height = 5

# Number of samples to show in "Sample Predictions"
sample_predictions = 10

# Decimal places for display
decimal_places = 6

# Correlation matrix format
heatmap_colormap = coolwarm

[DATA_DISPLAY]
# Settings for data display

# Number of rows in header preview
preview_rows = 5

# Download button label
download_button = Download CSV

[MESSAGES]
# Custom messages and text

# Dashboard title
dashboard_title = LSI Predictive AI Model

# Dashboard subtitle
dashboard_subtitle = Langelier Saturation Index Prediction Dashboard

# Prediction button label
predict_button = Predict LSI

# Model description
model_description = This model predicts LSI values based on water quality parameters

[FEATURES]
# Enable/disable dashboard features

enable_predictions = true
enable_model_analysis = true
enable_historical_data = true
enable_csv_download = true
enable_correlation_analysis = true
enable_predictions_caching = true

[PERFORMANCE]
# Performance optimization settings

# Use caching for data and models
use_caching = true

# Cache expiration time (in hours)
cache_expiration = 24

# Number of cross-validation folds
cv_folds = 5

# Show execution time
show_timing = false

[COLORS]
# Custom color scheme

# Primary color (charts)
primary_color = #1f77b4

# Secondary color
secondary_color = #ff7f0e

# Success color
success_color = #2ca02c

# Warning color
warning_color = #ff0000

# Background color
background_color = #f0f2f6

[ADVANCED]
# Advanced settings

# Log level (DEBUG, INFO, WARNING, ERROR)
log_level = INFO

# Enable debug mode
debug_mode = false

# Show performance metrics
show_metrics = true

# Batch prediction mode (number of samples)
batch_prediction_size = 100

# Model retraining frequency (days)
retrain_frequency = 30

# Data quality check
perform_quality_check = true

---

# HOW TO USE THIS FILE:
# 1. This file is for reference and customization
# 2. To change values, edit the corresponding parameter
# 3. The main app uses hardcoded values (this is a template)
# 4. To implement these settings in the app, you would:
#    - Install: pip install configparser
#    - Add to app: import configparser
#    - Load: config = configparser.ConfigParser()
#    - Read: config.read('config.ini')

---

# EXAMPLE OF RECOMMENDED CHANGES:
# - Change port if 8501 is in use
# - Adjust LSI thresholds based on your system
# - Customize colors to match your branding
# - Enable/disable features as needed
